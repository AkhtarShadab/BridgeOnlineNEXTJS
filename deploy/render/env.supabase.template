# Paste into Render → Environment (replace YOUR_DB_PASSWORD).
# Password: https://supabase.com/dashboard/project/zxglihcentjhwwxqkprb/settings/database
#
# IMPORTANT: Use the Supabase *pooler* host — NOT db.*.supabase.co.
# Render (IPv4) often cannot reach the direct DB host (IPv6) → Prisma P1001.
#
# Do NOT commit a filled copy of this file.

# Runtime (Transaction pooler, port 6543) — Prisma + PgBouncer
DATABASE_URL=postgresql://postgres.zxglihcentjhwwxqkprb:YOUR_DB_PASSWORD@aws-0-ap-south-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1

# DirectUrl for Prisma (Session pooler, port 5432) — same pooler host, NOT db.* 
DIRECT_URL=postgresql://postgres.zxglihcentjhwwxqkprb:YOUR_DB_PASSWORD@aws-0-ap-south-1.pooler.supabase.com:5432/postgres

NEXTAUTH_URL=https://bridgeonline.onrender.com
# NEXT_PUBLIC_SOCKET_URL optional (defaults to same origin)
